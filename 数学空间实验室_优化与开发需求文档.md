# 数学空间实验室（Math Space Lab）功能优化与开发需求文档

> 文档版本：V1.0  
> 项目类型：纯前端数学互动实验平台  
> 当前技术栈：Vue 3 + Vite 7 + Three.js  
> 部署模式：纯静态部署  
> 后端：不开发服务端  
> 数据存储：IndexedDB + Dexie.js  
> 数据备份：JSON 导入/导出  
> 数据分享：URL State  
> 离线能力：PWA  
> 核心原则：在现有项目基础上增量开发，不破坏已有功能，不进行无必要的大规模重构。

---

# 1. 项目定位

## 1.1 项目目标

将现有的「数学空间实验室」从一个以数学知识展示、3D 几何展示和小游戏为主的前端项目，升级为：

> 一个可以让学生进行「观察 → 操作 → 猜想 → 实验 → 计算 → 验证 → 记录 → 挑战」的互动数学实验平台。

项目重点不是简单增加数学题，而是通过：

- Three.js 3D 场景
- 动态几何
- 参数调整
- 数学测量
- 几何变换
- 实验过程记录
- 实验结果验证
- 游戏化挑战

帮助学生理解数学概念。

---

# 2. 当前项目架构

当前项目为 Vue 3 + Vite + Three.js 纯前端 SPA。

基本入口：

```text
index.html
    ↓
src/main.js
    ↓
App.vue
```

当前原则：

- 不强制引入 vue-router
- 不强制引入 Pinia
- 不开发后端
- 不开发 API 服务
- 不使用服务器数据库
- 不改变现有实验室访问方式
- 保持现有 URL 参数切换机制

---

# 3. 现有实验室

当前已经存在以下实验室，必须保留并保证兼容。

## 3.1 GeometryLab

当前主要功能：

1. 组合 / 多视图
2. 单体展开图
3. 三视图挑战
4. 涂色分割
5. 结构探索
6. 搭积木
7. 展开图判断

GeometryLab 使用 Three.js 构建 3D 数学实验环境，是整个项目的核心能力。

---

## 3.2 FractionLab

分数实验室。

当前包含：

- 分数模型
- 分数比较
- 分数练习
- 分数计算
- 分数相关教学数据

---

## 3.3 AreaLab

面积实验室。

当前包含：

- 图形面积
- 面积公式
- 随机测验
- AreaGallery
- AreaDrive
- AreaQuiz
- AreaCompose
- AreaShape
- AreaTile

---

## 3.4 DecimalLab

小数实验室。

当前包含：

- 小数
- 数位网格
- 数线
- 加减法
- DecimalIntro
- DecimalGrid
- DecimalLine
- DecimalAdd
- DecFigure

---

## 3.5 UnitLab

单位换算实验室。

当前包含：

- 单位换算
- 换算阶梯
- 闯关
- 比大小
- 单位换算规则

---

## 3.6 GamesLab

数学益智游戏。

当前包含：

- 2048
- 数字华容道 / Slide Puzzle
- 24 点
- 数独

---

# 4. 总体产品方向

后续开发必须围绕以下核心学习闭环：

```text
观察
 ↓
操作
 ↓
猜想
 ↓
实验
 ↓
计算
 ↓
验证
 ↓
记录
 ↓
挑战
```

不要单纯堆叠题目页面。

优先建设：

- 可操作数学对象
- 动态数学模型
- 实验过程
- 参数变化
- 数学关系验证
- 学习数据记录

---

# 5. 新增功能一：参数化几何体系统

## 5.1 目标

让几何体不再只是静态模型，而是成为可以调整参数、实时计算数学属性的动态数学对象。

---

## 5.2 支持的几何体

第一阶段至少支持：

- 点
- 线
- 三角形
- 正方形
- 长方形
- 圆
- 立方体
- 长方体
- 圆柱
- 圆锥
- 球
- 棱柱
- 棱锥

---

## 5.3 参数控制

### 立方体

参数：

```text
边长 a
```

实时计算：

```text
表面积 = 6a²
体积 = a³
空间对角线 = √3a
```

### 长方体

参数：

```text
长 a
宽 b
高 c
```

实时计算：

```text
表面积 = 2(ab + ac + bc)
体积 = abc
空间对角线 = √(a²+b²+c²)
```

### 圆柱

参数：

```text
半径 r
高度 h
```

实时计算：

```text
底面积 = πr²
侧面积 = 2πrh
表面积 = 2πr(r+h)
体积 = πr²h
```

---

## 5.4 参数 UI

建议支持：

- Slider
- Number Input
- +/- 按钮

参数变化必须实时同步：

```text
参数
 ↓
Three.js 模型
 ↓
数学计算
 ↓
结果展示
```

---

# 6. 新增功能二：动态几何实验室

新增：

```text
DynamicGeometryLab
```

目标：

让用户拖动点、线、几何体，并实时观察数学属性变化。

---

## 6.1 三角形实验

支持：

```text
拖动 A
拖动 B
拖动 C
```

实时显示：

```text
AB
BC
AC

∠A
∠B
∠C

周长
面积
```

---

## 6.2 动态约束

第一阶段支持：

```text
AB = AC
AB ⟂ BC
AB ∥ CD
∠A = ∠B
A、B、C 共线
```

拖动对象时必须保持约束。

---

## 6.3 约束系统

建议抽象：

```text
GeometryConstraint
```

例如：

```js
{
  type: 'equal-length',
  targetA: 'AB',
  targetB: 'AC'
}
```

后续可以扩展：

```text
parallel
perpendicular
equal-angle
collinear
fixed-length
fixed-angle
symmetry
```

---

# 7. 新增功能三：数学测量实验室

新增：

```text
MeasurementLab
```

目标：

让学生通过测量理解数学，而不是直接获得答案。

---

## 7.1 长度测量

提供虚拟尺子。

支持：

- 拖动起点
- 拖动终点
- 实时显示长度
- 估测
- 实际测量
- 误差计算

流程：

```text
估计长度
 ↓
输入答案
 ↓
实际测量
 ↓
计算误差
 ↓
评价
```

---

## 7.2 角度测量

提供虚拟量角器。

支持：

- 拖动射线
- 实时显示角度
- 判断锐角
- 判断直角
- 判断钝角
- 判断平角

---

## 7.3 面积测量

提供：

- 方格纸
- 单位正方形
- 图形拆分
- 图形拼接

流程：

```text
估计面积
 ↓
方格测量
 ↓
计算面积
 ↓
比较误差
```

---

## 7.4 体积测量

通过单位小方块构建立体图形。

单位：

```text
1 × 1 × 1
```

通过堆叠小方块计算体积。

---

# 8. 新增功能四：截面实验室

新增：

```text
SectionLab
```

目标：

通过 Three.js 动态展示立体图形被平面切割后的截面。

---

## 8.1 第一阶段支持

```text
正方体
长方体
圆柱
圆锥
棱柱
棱锥
球
```

---

## 8.2 截面平面

用户可以：

```text
拖动截面平面
```

实时显示：

```text
3D 模型
+
截面
+
截面二维图形
```

---

## 8.3 截面识别

系统至少支持：

```text
三角形
正方形
长方形
梯形
五边形
六边形
圆
椭圆
```

---

# 9. 新增功能五：空间切割实验

新增：

```text
CuttingLab
```

支持：

```text
一刀切
二刀切
三刀切
```

实验目标：

> 一个几何体最多可以被切成多少部分？

记录：

```text
切割次数
当前块数
理论最大块数
实际块数
```

支持学生先进行猜想，然后通过实验验证。

---

# 10. 新增功能六：动画证明实验室

新增：

```text
ProofLab
```

目标：

不要直接告诉学生公式，而是通过动画展示公式为什么成立。

---

## 10.1 三角形面积

动画流程：

```text
三角形
 ↓
复制
 ↓
旋转
 ↓
拼成平行四边形
 ↓
平行四边形面积 = 底 × 高
 ↓
三角形面积 = 底 × 高 ÷ 2
```

---

## 10.2 勾股定理

通过面积拼接展示：

```text
a² + b² = c²
```

---

## 10.3 圆面积

动画流程：

```text
圆
 ↓
切割成多个扇形
 ↓
交错排列
 ↓
逐渐接近平行四边形
 ↓
面积 = πr²
```

---

## 10.4 梯形面积

动画流程：

```text
梯形
 ↓
复制
 ↓
旋转
 ↓
拼成平行四边形
 ↓
推导面积公式
```

---

# 11. 新增核心基础设施：ExperimentEngine

这是整个后续系统的核心。

目录：

```text
src/core/experiment/
```

建议文件：

```text
ExperimentEngine.js
ExperimentState.js
ExperimentRecorder.js
ExperimentValidator.js
ExperimentCalculator.js
```

---

## 11.1 ExperimentEngine

负责：

```text
创建实验
初始化实验
加载模型
处理用户操作
更新实验状态
计算结果
验证结果
完成实验
重置实验
销毁实验
```

建议统一接口：

```js
createExperiment()
initialize()
start()
update()
validate()
complete()
reset()
destroy()
```

---

## 11.2 ExperimentState

统一保存实验状态。

示例：

```js
{
  experimentId: 'cube-volume',
  status: 'running',

  parameters: {
    width: 5,
    height: 5,
    depth: 5
  },

  result: {
    volume: 125,
    surfaceArea: 150
  },

  score: 80,

  startedAt: 0,
  completedAt: null
}
```

---

## 11.3 ExperimentRecorder

记录实验过程：

```text
开始实验
调整参数
拖动模型
回答问题
计算结果
完成实验
```

记录：

```text
操作时间
操作类型
操作参数
操作结果
```

---

## 11.4 ExperimentValidator

负责验证：

```text
答案
参数
实验结果
猜想
数学结论
```

---

# 12. 新增功能七：猜想 → 实验 → 验证

这是平台重要的教学功能。

---

## 12.1 实验流程

```text
提出问题
 ↓
学生猜想
 ↓
记录猜想
 ↓
调整参数
 ↓
执行实验
 ↓
观察结果
 ↓
计算
 ↓
验证猜想
```

---

## 12.2 示例

问题：

```text
正方体边长扩大 2 倍后，
体积扩大多少倍？
```

学生选择：

```text
2 倍
4 倍
6 倍
8 倍
```

实验：

```text
边长：
1 → 2

体积：
1³ → 2³

1 → 8
```

最终：

```text
猜想：8 倍
实验结果：8 倍

结论：
猜想正确
```

---

# 13. 新增本地数据存储系统

## 13.1 技术要求

必须使用：

```text
IndexedDB
+
Dexie.js
```

禁止开发：

```text
后端数据库
REST API
GraphQL
服务器 Session
```

---

## 13.2 数据库

数据库名称：

```text
MathSpace
```

建议 Store：

```text
experiments
progress
mistakes
achievements
games
favorites
reports
settings
```

---

## 13.3 Repository 模式

禁止 Vue 页面直接操作 IndexedDB。

必须：

```text
Vue Component
 ↓
Repository
 ↓
Dexie
 ↓
IndexedDB
```

示例：

```js
await experimentRepository.save(record)
```

查询：

```js
const records =
  await experimentRepository.list('cube-volume')
```

不要在 Vue 页面中直接：

```js
indexedDB.open(...)
```

---

# 14. 动态数据结构

## 14.1 experiments

记录实验：

```text
id
experimentId
status
parameters
result
score
startedAt
completedAt
duration
createdAt
updatedAt
```

---

## 14.2 progress

记录学习进度：

```text
id
experimentId
completed
completionCount
correctCount
wrongCount
bestScore
lastPlayedAt
```

---

## 14.3 mistakes

记录错题：

```text
id
questionId
experimentId
question
userAnswer
correctAnswer
wrongCount
firstWrongAt
lastWrongAt
mastered
```

---

## 14.4 achievements

记录成就：

```text
id
achievementId
unlocked
unlockedAt
```

---

## 14.5 games

记录游戏：

```text
id
gameId
score
level
duration
result
createdAt
```

---

## 14.6 favorites

记录收藏：

```text
id
type
targetId
createdAt
```

---

## 14.7 reports

记录实验报告：

```text
id
experimentId
title
purpose
parameters
process
result
hypothesis
conclusion
score
duration
createdAt
```

---

# 15. 学习进度

记录：

```text
实验完成数量
实验总数量
实验完成率
正确率
错误次数
实验耗时
连续学习天数
```

展示：

```text
我的学习

几何实验       90%
分数实验       70%
面积实验      100%
单位换算       60%
```

---

# 16. 错题系统

## 16.1 错题记录

记录：

```text
题目ID
实验ID
题目内容
用户答案
正确答案
错误次数
第一次错误时间
最近错误时间
是否已掌握
```

---

## 16.2 错题重新练习

流程：

```text
错题本
 ↓
重新练习
 ↓
答题
 ↓
正确
 ↓
降低错误权重
 ↓
连续正确
 ↓
标记为已掌握
```

---

# 17. 成就系统

新增：

```text
AchievementSystem
```

第一阶段成就：

```text
首次实验
完成 10 次实验
完成 50 次实验
连续学习 7 天
几何实验全部完成
完成 100 道题
首次满分
连续 10 题正确
```

成就数据：

```js
{
  achievementId: 'first-experiment',
  unlocked: true,
  unlockedAt: 0
}
```

---

# 18. 实验报告

每次实验完成后，可以生成实验报告。

报告包含：

```text
实验名称
实验目的
实验参数
实验过程
实验结果
数学计算
我的猜想
猜想结果
最终结论
实验耗时
实验得分
```

---

## 18.1 导出格式

第一阶段支持：

```text
JSON
Markdown
打印
```

后续可增加：

```text
PDF
```

---

# 19. GeometryBuilder 几何体构造器

新增：

```text
GeometryBuilder
```

---

## 19.1 左侧工具栏

```text
点
线
三角形
正方形
圆
立方体
长方体
圆柱
圆锥
球
棱柱
棱锥
```

---

## 19.2 中间 Three.js 场景

支持：

```text
旋转
缩放
平移
选择
拖动
删除
复制
```

---

## 19.3 右侧属性

```text
位置
旋转
缩放

尺寸

颜色
透明度
```

显示控制：

```text
□ 顶点
□ 棱
□ 面
□ 网格
□ 坐标轴
□ 数学标注
```

---

# 20. 3D ↔ 2D 双向联动

增强现有 ThreeViewQuiz。

---

## 20.1 3D → 2D

用户旋转 3D 模型：

```text
3D 模型
 ↓
正视图
左视图
俯视图
```

三视图实时变化。

---

## 20.2 2D → 3D

用户修改：

```text
正视图
左视图
俯视图
```

系统重新构建 3D 方块模型。

---

## 20.3 方块搭建挑战

例如给出：

```text
■■■
■□□
■■□
```

要求学生搭建对应 3D 模型。

---

# 21. FormulaLab

新增：

```text
FormulaLab
```

目标：

实现数学公式和图像的动态可视化。

第一阶段支持：

```text
一次函数
正比例函数
反比例函数
二次函数
圆
坐标系
斜率
两点距离
中点
对称
```

---

## 21.1 动态参数

例如：

```text
y = ax² + bx + c
```

提供：

```text
a Slider
b Slider
c Slider
```

参数变化时：

```text
公式
 ↓
函数图像
 ↓
坐标数据
```

同步变化。

---

# 22. URL State 实验分享

所有可以被参数描述的实验支持 URL State。

简单实验：

```text
/geometry?shape=cube&size=5
```

复杂实验可以：

```text
/geometry#压缩后的实验状态
```

---

## 22.1 分享流程

```text
创建实验
 ↓
配置参数
 ↓
点击分享
 ↓
生成 URL
 ↓
复制链接
```

其他用户打开：

```text
URL
 ↓
解析状态
 ↓
恢复实验
```

不需要服务器。

---

# 23. 本地数据导入 / 导出

因为数据全部存储在浏览器中，必须提供备份能力。

---

## 23.1 导出

文件：

```text
math-space-backup-YYYYMMDD.json
```

格式：

```json
{
  "version": 1,
  "exportTime": "2026-09-09T15:00:00",
  "progress": [],
  "experiments": [],
  "mistakes": [],
  "achievements": [],
  "games": [],
  "favorites": [],
  "reports": []
}
```

---

## 23.2 导入

流程：

```text
选择 JSON
 ↓
版本校验
 ↓
数据格式校验
 ↓
数据导入 IndexedDB
 ↓
恢复学习数据
```

---

## 23.3 数据版本迁移

必须设计：

```text
version
```

未来数据库结构变化：

```text
v1
 ↓
v2
 ↓
v3
```

支持 Migration。

---

# 24. PWA

增加 PWA 能力。

---

## 24.1 要求

支持：

```text
安装到桌面
Service Worker
静态资源缓存
离线打开
本地数据保存
```

---

## 24.2 离线模式

无网络情况下仍可以使用核心功能：

```text
GeometryLab
FractionLab
AreaLab
DecimalLab
UnitLab
GamesLab
```

---

# 25. 推荐最终目录结构

```text
src/
│
├── core/
│   │
│   ├── geometry/
│   │   ├── GeometryFactory.js
│   │   ├── GeometryController.js
│   │   ├── GeometryMeasure.js
│   │   ├── GeometryTransform.js
│   │   └── GeometryConstraint.js
│   │
│   ├── experiment/
│   │   ├── ExperimentEngine.js
│   │   ├── ExperimentState.js
│   │   ├── ExperimentRecorder.js
│   │   ├── ExperimentValidator.js
│   │   └── ExperimentCalculator.js
│   │
│   └── quiz/
│       ├── QuizEngine.js
│       └── AnswerValidator.js
│
├── components/
│   ├── common/
│   ├── geometry/
│   ├── experiment/
│   └── quiz/
│
├── labs/
│   ├── geometry/
│   ├── fractions/
│   ├── area/
│   ├── decimal/
│   ├── unit/
│   ├── games/
│   ├── measurement/
│   ├── section/
│   ├── cutting/
│   ├── proof/
│   ├── dynamic-geometry/
│   └── formula/
│
├── storage/
│   ├── db.js
│   ├── experimentRepository.js
│   ├── progressRepository.js
│   ├── mistakeRepository.js
│   ├── achievementRepository.js
│   ├── gameRepository.js
│   ├── favoriteRepository.js
│   ├── reportRepository.js
│   └── backupService.js
│
├── data/
│   ├── geometry/
│   ├── fractions/
│   ├── decimals/
│   ├── area/
│   ├── units/
│   ├── quizzes/
│   └── achievements/
│
├── utils/
│   ├── urlState.js
│   ├── math.js
│   ├── storage.js
│   └── export.js
│
└── App.vue
```

---

# 26. 数据架构原则

必须严格区分静态数据、动态数据和分享数据。

## 26.1 静态数据

位置：

```text
src/data/
```

包括：

- 教学数据
- 题库
- 几何体定义
- 单位换算规则
- 实验配置
- 成就定义

---

## 26.2 动态数据

位置：

```text
IndexedDB
```

包括：

- 学习进度
- 实验记录
- 错题
- 游戏记录
- 收藏
- 成就
- 实验报告
- 用户设置

---

## 26.3 分享数据

位置：

```text
URL State
```

包括：

- 当前几何体参数
- 当前实验参数
- 当前场景
- 可分享的实验状态

---

# 27. Three.js 与 Vue 解耦

Three.js 负责：

```text
3D 场景
模型
动画
交互
渲染
```

Vue 负责：

```text
UI
参数
菜单
题目
结果
状态展示
```

避免在 Vue Template 中直接实现复杂 Three.js 逻辑。

---

# 28. 实验统一接口

未来每个实验尽可能遵循：

```js
createExperiment()
initialize()
start()
update()
validate()
complete()
reset()
destroy()
```

实验页面不应该自行定义完全不同的生命周期。

---

# 29. 性能要求

由于 Three.js 是核心技术，必须重点防止 WebGL 和事件泄漏。

实验离开时必须：

1. 销毁 Three.js 场景。
2. 销毁 renderer。
3. 销毁 controls。
4. 释放 geometry。
5. 释放 material。
6. 释放 texture。
7. 取消 requestAnimationFrame。
8. 取消事件监听。
9. 防止重复初始化。
10. 防止 WebGL Context 泄漏。

---

# 30. 移动端适配

必须支持：

```text
PC
Tablet
Mobile
```

移动端 3D 操作：

```text
单指旋转
双指缩放
双指平移
```

UI 不得依赖 Hover。

避免：

```text
固定宽度
过小按钮
过小字体
Hover-only 操作
```

---

# 31. 统一实验 UI

建议所有实验统一为：

```text
┌───────────────────────────────────┐
│ 实验标题                           │
├──────────────┬────────────────────┤
│              │                    │
│   工具栏      │     实验场景        │
│              │                    │
│              │                    │
├──────────────┴────────────────────┤
│ 参数 / 实验控制                     │
├───────────────────────────────────┤
│ 数学结果 / 问题 / 提示               │
└───────────────────────────────────┘
```

所有实验保持统一的视觉语言和交互方式。

---

# 32. 错误处理

所有实验必须处理：

```text
初始化失败
WebGL 不可用
IndexedDB 不可用
数据读取失败
导入数据格式错误
URL 参数错误
实验状态损坏
```

单个实验失败不得导致整个 App 崩溃。

---

# 33. 隐私原则

由于项目没有服务端：

> 所有学习数据默认只保存在用户自己的浏览器中。

不得：

- 自动上传学习数据
- 自动上传实验记录
- 自动上传成绩
- 自动引入无必要的数据统计服务

数据导出必须由用户主动操作。

---

# 34. 开发阶段

## Phase 1：基础设施

优先完成：

```text
IndexedDB
Dexie.js
Repository
ExperimentEngine
ExperimentState
ExperimentRecorder
JSON 导入
JSON 导出
URL State
```

这一阶段暂时不要大量新增实验。

---

## Phase 2：核心数学实验能力

完成：

```text
参数化几何体
动态几何
数学测量
截面实验
空间切割
```

---

## Phase 3：学习闭环

完成：

```text
猜想
实验
验证
学习进度
错题
实验记录
实验报告
```

---

## Phase 4：空间数学增强

完成：

```text
GeometryBuilder
3D ↔ 2D
三视图双向编辑
FormulaLab
```

---

## Phase 5：游戏化

完成：

```text
等级
经验值
成就
勋章
每日挑战
连续学习
实验挑战
```

---

## Phase 6：PWA

完成：

```text
Service Worker
离线缓存
桌面安装
离线实验
```

---

# 35. 功能优先级

## P0：必须完成

```text
IndexedDB
Dexie.js
Repository
实验状态管理
参数化几何体
实验记录
JSON 导入/导出
```

## P1：核心竞争力

```text
动态几何
数学测量
截面
空间切割
猜想 → 实验 → 验证
3D ↔ 2D
GeometryBuilder
```

## P2：学习体验

```text
错题
学习进度
实验报告
成就
游戏化
```

## P3：增强能力

```text
FormulaLab
PWA
实验分享
更多动画证明
更多数学实验
```

---

# 36. 暂不开发

当前明确不开发：

```text
用户登录
服务器
MySQL
Redis
REST API
GraphQL
后台管理系统
教师后台
在线排行榜服务器
云端同步
复杂权限系统
```

原因：

> 当前项目定位为纯前端、纯静态、离线优先的数学实验平台。

如果未来需要教师查看学生数据，再单独设计云端同步层。

---

# 37. 为未来服务端预留扩展能力

当前：

```text
Vue
 ↓
Repository
 ↓
Dexie
 ↓
IndexedDB
```

未来可以扩展：

```text
Vue
 ↓
Repository
 ↓
┌─────────────────┐
│ Local Repository│
│ IndexedDB       │
└─────────────────┘
        +
┌─────────────────┐
│ Remote Repository│
│ API              │
└─────────────────┘
```

Vue 和实验室业务代码不能直接依赖 IndexedDB。

这样未来即使增加服务器，也不需要重写实验室。

---

# 38. AI Coding Agent 开发规则

AI 在实际修改项目之前，必须先检查现有代码，不得仅根据本文档假设项目结构。

至少检查：

```text
package.json
src/main.js
src/App.vue
src/labs
src/components
src/data
src/utils
```

---

## 38.1 不得破坏已有实验

以下实验必须保持正常：

```text
FractionLab
AreaLab
DecimalLab
UnitLab
GamesLab
GeometryLab
```

---

## 38.2 优先复用

发现已有实现时：

```text
优先复用
 ↓
必要时抽象
 ↓
最后才新建
```

不要重复实现已有功能。

---

## 38.3 增量开发

每完成一个阶段必须执行：

```bash
npm run build
```

确保项目可以正常构建。

---

## 38.4 不随意升级依赖

除非必要，不得随意升级：

```text
Vue
Vite
Three.js
```

版本。

新增依赖必须有明确理由。

---

## 38.5 不引入大型依赖

新增依赖应尽量轻量。

IndexedDB 推荐：

```text
Dexie.js
```

除非明确需要，不引入大型状态管理、UI 框架或服务端框架。

---

# 39. 验收标准

## 39.1 基础功能

- [ ] npm run dev 正常
- [ ] npm run build 正常
- [ ] 所有现有实验正常
- [ ] 页面切换正常
- [ ] URL 参数实验正常

---

## 39.2 数据功能

- [ ] IndexedDB 正常初始化
- [ ] 实验记录可以保存
- [ ] 学习进度可以保存
- [ ] 错题可以保存
- [ ] 游戏成绩可以保存
- [ ] 数据可以导出
- [ ] 数据可以导入
- [ ] 数据版本可以迁移

---

## 39.3 几何功能

- [ ] 几何体参数可以实时修改
- [ ] Three.js 模型实时更新
- [ ] 数学计算实时更新
- [ ] 动态几何拖拽正常
- [ ] 截面正常
- [ ] 空间切割正常
- [ ] 三视图正常

---

## 39.4 实验功能

- [ ] 可以开始实验
- [ ] 可以记录实验过程
- [ ] 可以验证答案
- [ ] 可以记录猜想
- [ ] 可以生成实验结果
- [ ] 可以生成实验报告

---

## 39.5 离线功能

- [ ] 首次访问可以正常加载
- [ ] 安装 PWA 后可以打开
- [ ] 断网后核心实验仍可使用
- [ ] 本地数据仍可读取
- [ ] 本地数据仍可保存

---

# 40. 最终产品结构

```text
数学空间实验室
│
├── 首页
│
├── 实验室
│   │
│   ├── GeometryLab
│   │   ├── 组合
│   │   ├── 展开
│   │   ├── 三视图
│   │   ├── 截面
│   │   ├── 切割
│   │   ├── 涂色
│   │   ├── 结构
│   │   ├── 搭积木
│   │   ├── 动态几何
│   │   └── 测量
│   │
│   ├── FractionLab
│   ├── AreaLab
│   ├── DecimalLab
│   ├── UnitLab
│   ├── FormulaLab
│   ├── MeasurementLab
│   ├── ProofLab
│   └── GamesLab
│
├── 挑战中心
│   ├── 几何挑战
│   ├── 数学游戏
│   ├── 每日挑战
│   └── 成就
│
└── 我的学习
    ├── 学习进度
    ├── 实验记录
    ├── 错题
    ├── 收藏
    └── 实验报告
```

---

# 41. 最终技术架构

```text
                         数学空间实验室
                                │
          ┌─────────────────────┼─────────────────────┐
          │                     │                     │
        实验室                  挑战中心              我的学习
          │                     │                     │
          │                     │                     ├── 学习进度
          │                     ├── 几何挑战           ├── 实验记录
          │                     ├── 数学游戏           ├── 错题
          │                     ├── 每日挑战           ├── 收藏
          │                     └── 成就               └── 实验报告
          │
          ├── GeometryLab
          ├── FractionLab
          ├── AreaLab
          ├── DecimalLab
          ├── UnitLab
          ├── FormulaLab
          ├── MeasurementLab
          ├── ProofLab
          └── GamesLab
                                │
                                ↓
                       ExperimentEngine
                                │
                  ┌─────────────┼─────────────┐
                  ↓             ↓             ↓
               Three.js       Quiz          Math Engine
                  │             │             │
                  └─────────────┼─────────────┘
                                ↓
                       ExperimentRecorder
                                │
                                ↓
                           Repository
                                │
                                ↓
                            Dexie.js
                                │
                                ↓
                           IndexedDB
                                │
                 ┌──────────────┴──────────────┐
                 ↓                             ↓
             JSON Backup                   URL State
                 │                             │
                 ↓                             ↓
             数据迁移                       实验分享
                                │
                                ↓
                               PWA
                                │
                                ↓
                         离线数学实验平台
```

---

# 42. 核心设计理念

本项目最终不是：

> 一个包含很多数学题的网页。

而应该是：

> 一个可以让学生亲手操作数学对象、观察数学规律、提出猜想、进行实验并验证结论的数字化数学实验室。

核心体验：

```text
看见数学
   ↓
操作数学
   ↓
改变参数
   ↓
观察变化
   ↓
提出猜想
   ↓
实验验证
   ↓
得出结论
   ↓
保存实验
   ↓
获得挑战
```

核心技术：

```text
Vue 3
+
Three.js
+
Math Engine
+
Experiment Engine
+
Dexie.js
+
IndexedDB
+
PWA
+
URL State
```

核心原则：

```text
纯前端
无服务端
离线优先
数据本地化
实验可分享
架构可扩展
不破坏现有功能
```

最终目标：

> 让“数学”从一道需要计算的题，变成一个可以被观察、操作和验证的对象。

---

# 43. AI 实施指令

你是本项目的 Coding Agent。

执行本需求时必须遵循以下顺序：

## Step 1：分析现有项目

先读取现有：

```text
package.json
src/main.js
src/App.vue
src/labs/**
src/components/**
src/data/**
src/utils/**
```

确认当前真实结构。

不要假设文件一定存在。

---

## Step 2：建立基础设施

首先实现：

```text
src/storage/
src/core/experiment/
```

完成：

```text
Dexie 初始化
Repository
ExperimentState
ExperimentEngine
ExperimentRecorder
数据导入/导出
```

确保：

```bash
npm run build
```

成功。

---

## Step 3：实现参数化几何体

不要重写现有 GeometryLab。

应该：

```text
复用现有 Three.js 几何体能力
 ↓
抽象参数系统
 ↓
增加实时数学计算
 ↓
接入 ExperimentEngine
```

---

## Step 4：逐步增加实验

按照：

```text
动态几何
 ↓
测量
 ↓
截面
 ↓
空间切割
 ↓
动画证明
```

逐项开发。

每完成一个功能：

```bash
npm run build
```

---

## Step 5：接入学习数据

所有新增实验必须接入：

```text
ExperimentRecorder
Repository
IndexedDB
```

至少记录：

```text
实验开始
实验完成
实验参数
实验结果
实验得分
实验耗时
```

---

## Step 6：实现学习闭环

加入：

```text
猜想
实验
验证
学习进度
错题
实验报告
```

---

## Step 7：增强 GeometryLab

增加：

```text
GeometryBuilder
3D ↔ 2D
三视图双向编辑
```

---

## Step 8：最后增加 PWA

只有核心功能稳定后再增加：

```text
Service Worker
离线缓存
安装能力
```

---

# 44. 重要约束

AI 不得：

1. 为本项目开发服务端。
2. 引入 MySQL。
3. 引入 Redis。
4. 创建 REST API。
5. 重写整个 App.vue。
6. 删除已有实验室。
7. 删除已有数据。
8. 随意升级核心依赖。
9. 为一个小功能引入大型依赖。
10. 将 IndexedDB 操作散落到各个 Vue 页面。
11. 在不同实验中重复实现相同的实验状态管理。
12. 为了新功能破坏已有 URL 参数访问方式。

如果发现现有代码结构与本文档不同：

> 以现有真实代码为准，在不破坏现有功能的前提下进行兼容性改造。

---

# 45. 开发完成后的目标

最终项目应该能够在没有任何服务器的情况下：

```text
npm run build
        ↓
生成 dist/
        ↓
部署到静态服务器/CDN
        ↓
用户访问
        ↓
使用数学实验
        ↓
数据保存到浏览器 IndexedDB
        ↓
断网仍可以使用核心实验
        ↓
导出 JSON 备份
        ↓
通过 JSON 恢复数据
        ↓
通过 URL 分享实验
```

最终形成一个：

> **纯前端、离线优先、可交互、可实验、可记录、可分享的数学实验平台。**
