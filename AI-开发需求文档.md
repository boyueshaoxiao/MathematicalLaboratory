# 几何空间实验室——纯前端 AI 开发需求文档

## 1. 项目概述

### 1.1 产品名称

几何空间实验室（Geometry Space Lab）

### 1.2 产品定位

一个面向小学生的纯前端 3D 几何互动工具。

核心不是题库、考试或学习数据管理，而是通过实时 3D 渲染，让孩子直接观察：

- 不同几何图形
- 同一个几何体从不同方向看到的视图
- 多个不同几何体组合后的空间结构
- 立体图形的展开与折叠
- 面、棱、顶点等结构

### 1.3 核心原则

1. 纯前端运行。
2. 不需要后端。
3. 不需要数据库。
4. 不需要登录。
5. 不需要用户数据。
6. 不需要联网 API。
7. 图形和组合体均由 Three.js 动态生成。
8. 所有操作实时反馈。
9. UI 面向小学生，少文字、大图形、强视觉反馈。
10. 优先保证 3D 交互流畅和空间观察体验。

---

## 2. 技术要求

- Vue 3
- Vite
- Three.js
- JavaScript / ES Module
- CSS
- 不使用 TypeScript 也可以。
- 不引入后端。
- 不引入数据库。
- 不依赖登录服务。
- 不依赖远程 API。

推荐目录：

```text
geometry-space-lab/
├── src/
│   ├── components/
│   │   ├── GeometryViewer.vue
│   │   ├── ShapeSelector.vue
│   │   ├── ViewControls.vue
│   │   └── ObjectInspector.vue
│   ├── geometry/
│   │   ├── factory.js
│   │   ├── primitives.js
│   │   ├── prism.js
│   │   ├── pyramid.js
│   │   └── unfold.js
│   ├── data/
│   │   └── shapes.js
│   ├── views/
│   │   └── Home.vue
│   ├── App.vue
│   ├── main.js
│   └── style.css
├── index.html
├── package.json
└── vite.config.js
```

---

## 3. 几何图形库

### 3.1 基础立体

必须支持：

- 正方体
- 长方体
- 球体
- 半球
- 圆柱
- 圆锥
- 圆台
- 圆环

### 3.2 棱柱

采用参数化模型，不要为每种棱柱复制一套代码。

支持：

- 三棱柱
- 四棱柱
- 五棱柱
- 六棱柱
- 八棱柱
- N 棱柱

参数：

```text
sides
radius
height
```

### 3.3 棱锥

参数化生成：

- 三棱锥
- 四棱锥
- 五棱锥
- 六棱锥
- N 棱锥

参数：

```text
sides
radius
height
```

### 3.4 平面图形

为后续展开功能保留统一的数据结构，支持：

- 圆
- 三角形
- 正方形
- 长方形
- 菱形
- 平行四边形
- 梯形
- N 边形
- 椭圆

---

## 4. 图形数据模型

每个场景对象建议采用：

```js
{
  id: 1,
  type: "box",
  name: "正方体",
  params: {
    size: 2
  },
  position: {
    x: 0,
    y: 0,
    z: 0
  },
  rotation: {
    x: 0,
    y: 0,
    z: 0
  },
  scale: 1,
  color: 0x4f8cff,
  showEdges: true
}
```

重点：几何参数和场景变换参数分离。

---

## 5. 核心功能

## 5.1 几何图形库

左侧显示图形分类。

用户点击一个图形后：

1. 创建对应 Three.js Geometry。
2. 添加到 3D 场景。
3. 自动选择该对象。
4. 自动进入自由观察模式。

---

## 5.2 3D 自由观察

必须支持：

- 鼠标拖动旋转
- 滚轮缩放
- OrbitControls
- 平滑阻尼
- 自适应窗口大小

移动端需要支持触摸操作。

---

## 5.3 场景组合

允许同时添加多个不同几何图形。

例如：

```text
圆柱 + 圆锥
长方体 + 球
正方体 + 三棱柱
圆柱 + 圆锥 + 球
```

每个对象独立拥有：

- 位置
- 旋转
- 缩放
- 类型
- 参数

最终形成组合体。

---

## 5.4 对象属性编辑

选中场景对象后可以调整：

- X
- Y
- Z
- Rotation X
- Rotation Y
- Rotation Z
- Scale

修改属性后 3D 模型实时更新。

---

## 5.5 六方向视图

必须提供：

- 自由视图
- 前视图
- 后视图
- 左视图
- 右视图
- 俯视图
- 仰视图

点击方向按钮后：

1. 摄像机移动到对应方向。
2. 目标点保持在场景中心。
3. 使用平滑动画过渡。
4. 组合体保持不变。

---

## 5.6 多视图

第二阶段增加工程制图式多视图：

```text
┌──────────────┬──────────────┐
│              │              │
│     3D       │    俯视图    │
│              │              │
├──────────────┼──────────────┤
│    正视图    │    侧视图    │
│              │              │
└──────────────┴──────────────┘
```

四个视图同步显示同一个场景。

3D 场景旋转时，正视/侧视/俯视保持固定方向。

---

## 5.7 辅助显示

支持开关：

- 网格
- 棱线
- 顶点
- 面
- 半透明
- 线框

第一版至少实现：

- 网格
- 棱线
- 半透明

---

## 5.8 展开与折叠

第一阶段至少支持：

- 正方体展开
- 长方体展开
- 三棱柱展开
- 四棱柱展开

要求：

- 3D → 平面
- 平面 → 3D
- 动画过渡
- 可以重复展开/折叠

不要第一版实现任意复杂组合体自动展开。

---

## 5.9 自由搭积木

第二阶段实现。

用户可以添加大量小立方体：

```text
■
■■
■■■
```

支持：

- 添加
- 删除
- 移动
- 网格吸附
- 自动对齐
- 旋转
- 六视图

重点用于空间想象训练。

---

## 6. UI 设计

整体采用轻量、明亮、现代的儿童教育风格。

避免：

- 大量文字
- 复杂菜单
- 传统后台风格
- 过度卡通化
- 复杂表单

核心页面：

```text
┌───────────────────────────────────────────────┐
│ 🔷 几何空间实验室                 [重置]      │
├─────────────┬───────────────────┬─────────────┤
│ 几何图形    │                   │ 对象属性    │
│             │                   │             │
│ 正方体      │                   │ 位置        │
│ 长方体      │      3D 工作区    │ 旋转        │
│ 球          │                   │ 缩放        │
│ 圆柱        │                   │             │
│ 圆锥        │                   │             │
│ 棱柱        │                   │             │
│ 棱锥        │                   │             │
├─────────────┴───────────────────┴─────────────┤
│ 自由 前 后 左 右 上 下 | 棱线 半透明 网格       │
├───────────────────────────────────────────────┤
│ 场景对象：正方体 / 圆柱 / 圆锥                 │
└───────────────────────────────────────────────┘
```

---

## 7. 交互规则

### 添加图形

点击左侧图形：

```text
选择图形
→ 创建模型
→ 加入场景
→ 自动选中
```

### 删除

从场景对象列表删除。

### 选择

点击场景对象列表选中对象。

### 旋转

鼠标拖动 3D 工作区。

### 缩放

鼠标滚轮。

### 视图

点击方向按钮切换摄像机。

### 重置

清空所有场景对象并恢复自由视角。

---

## 8. Three.js 要求

使用：

```text
Scene
PerspectiveCamera
WebGLRenderer
OrbitControls
Ambient/Hemisphere Light
DirectionalLight
GridHelper
Mesh
MeshStandardMaterial
EdgesGeometry
LineSegments
```

要求：

- 场景管理统一。
- 几何体创建统一通过 GeometryFactory。
- 不允许在 Vue 模板中直接创建 Three.js 对象。
- Vue 负责 UI 和状态。
- Three.js 负责 3D 渲染。

---

## 9. 几何工厂

建议：

```js
createGeometry(shape)
```

统一入口。

例如：

```js
createGeometry({
  type: "box",
  params: { size: 2 }
})

createGeometry({
  type: "prism",
  params: {
    sides: 6,
    radius: 1.2,
    height: 2
  }
})
```

返回 Three.js Geometry。

---

## 10. 性能要求

因为是纯前端：

- 控制模型面数。
- 不使用超高精度几何。
- renderer 开启 antialias。
- pixelRatio 最大限制为 2。
- 避免无意义的实时重建 Geometry。
- 对未使用的 Geometry/Material 正确 dispose。
- 场景对象数量默认控制在合理范围。

---

## 11. 明确不做

当前版本明确不实现：

- 用户登录
- 用户注册
- 后端
- API
- 数据库
- 用户数据
- 学习记录
- 排行榜
- 账号体系
- 家长端
- 管理后台
- 在线题库
- 云端存档
- AI 对话

整个应用应该可以：

```text
npm install
npm run dev
```

直接运行。

生产环境：

```text
npm run build
```

生成静态文件后可以直接部署到：

- Nginx
- IIS
- GitHub Pages
- 任意静态网站空间

---

## 12. 开发优先级

### P0

必须完成：

1. Vue 3 + Vite
2. Three.js
3. 几何图形库
4. 正方体
5. 长方体
6. 球
7. 圆柱
8. 圆锥
9. 圆台
10. 圆环
11. N 棱柱
12. N 棱锥
13. 3D 自由旋转
14. 多对象组合
15. 对象移动/旋转/缩放
16. 前后左右上下视图
17. 网格
18. 棱线
19. 半透明
20. 重置场景

### P1

1. 多视图同步
2. 顶点显示
3. 面显示
4. 参数编辑
5. 图形尺寸显示
6. 正方体展开
7. 长方体展开
8. 三棱柱展开
9. 四棱柱展开

### P2

1. 自由搭积木
2. 网格吸附
3. 自动对齐
4. 更多展开图
5. 截面
6. 旋转体
7. 更复杂组合体
8. 视图辅助线

---

## 13. 最终验收标准

打开网页后，用户不需要任何账号或配置即可：

1. 看到 3D 工作区。
2. 点击正方体即可看到正方体。
3. 可以鼠标旋转。
4. 可以缩放。
5. 可以添加第二个不同图形。
6. 两个图形可以同时存在。
7. 可以调整两个图形的位置。
8. 可以调整旋转。
9. 可以调整缩放。
10. 可以切换前后左右上下视图。
11. 可以打开/关闭棱线。
12. 可以打开/关闭半透明。
13. 可以打开/关闭网格。
14. 可以一键清空场景。
15. 页面不需要后端即可完整运行。

---

## 14. AI 开发执行要求

如果由 AI Coding Agent 实现：

### 第一阶段

先完成：

```text
项目初始化
↓
Three.js 场景
↓
GeometryFactory
↓
基础图形
↓
组合
↓
旋转
↓
六视图
↓
对象属性编辑
```

### 第二阶段

再完成：

```text
多视图
↓
展开/折叠
↓
更多几何体
↓
辅助显示
```

### 第三阶段

最后完成：

```text
自由搭积木
↓
吸附
↓
复杂组合体
```

不要在第一阶段加入后端、数据库或用户系统。


---

## 8. 单个几何体多种展开方式模块（新增）

### 8.1 目标
在已有“组合与多视图”基础上增加“单体展开图”学习模式。用户选择一个几何体后，可以选择不同的展开方式，并通过滑杆或自动动画观察立体逐渐展开成平面展开图。

### 8.2 支持图形
第一阶段：
- 正方体：至少 3 种展开方式
- 长方体：至少 2 种展开方式
- 三棱柱：至少 2 种展开方式
- 圆柱：至少 2 种展示方式
- 圆锥：至少 2 种展示方式
- 四棱锥：至少 2 种展开方式

### 8.3 核心交互
1. 选择几何体。
2. 选择展开方式。
3. “折叠”按钮：恢复到立体状态。
4. “自动展开”按钮：从当前状态逐渐展开。
5. “完全展开”按钮：直接到 100%。
6. 展开程度滑杆：0%～100%。
7. 鼠标拖动旋转、滚轮缩放。
8. 面、棱、折痕使用不同视觉效果。
9. 切换展开方式后自动重置动画状态。

### 8.4 教学重点
- 一个立体图形可能存在多种不同展开图。
- 展开图中的每一个平面都对应立体图形的一个面。
- 展开与折叠本质上是围绕公共棱旋转。
- 圆柱：侧面展开为长方形，两个底面为圆。
- 圆锥：侧面展开为扇形，底面为圆。
- 棱柱、棱锥可以观察底面与侧面的对应关系。

### 8.5 后续增强
- 正方体 11 种标准展开图。
- 长方体合法展开图生成器。
- 三棱柱、五棱柱、六棱柱等自动生成展开图。
- 折痕线高亮。
- 面编号、顶点编号。
- 展开图拖拽与吸附。
- “给展开图，判断能否折成立体”互动题。
- “看立体，选择正确展开图”选择题。
- “看展开图，猜立体”训练模式。
